import 'package:flutter/material.dart';

class StatusTime extends StatelessWidget {
  const StatusTime({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 44);
  }
}
